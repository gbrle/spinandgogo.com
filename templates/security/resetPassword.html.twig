{% block body %}
    <div>
        <div class="container bignoode">
            <div class="col-12">
                <h1 class="tittlePage mt-5 mb-5 animated bounceInLeft display-3"><span class="text-danger">R</span>éinitialiser mot de passe</h1>
            </div>

            {{ form_start(form) }}
            <div class="row">
                <div class="col-md-6">
                    {{ form_row(form.password) }}
                </div>
                <div class="col-md-6">
                    {{ form_row(form.confirm_password) }}
                </div>
            </div>
            <button type="submit" class="btn btn-danger btn-lg float-md-right mt-4 mb-4">Valider</button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}